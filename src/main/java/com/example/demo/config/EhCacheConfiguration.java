package com.example.demo.config;

/*
 * @Configuration public class EhCacheConfiguration {
 * 
 * @Bean public EhCacheManagerFactoryBean cacheManager() { return new
 * EhCacheManagerFactoryBean(); }
 * 
 * @Bean public EhCacheCacheManager testEhCacheManager() { CacheConfiguration
 * cacheConfig = new CacheConfiguration().eternal(false) // if true, timeouts
 * are ignored .timeToIdleSeconds(10) // time since last accessed before item is
 * marked for removal .timeToLiveSeconds(5) // time since inserted before item
 * is marked for removal .maxEntriesLocalHeap(10) // total items that can be
 * stored in cache .memoryStoreEvictionPolicy("LRU") // eviction policy for when
 * items exceed cache. LRU = Least Recently // Used .name("testCache"); Cache
 * testCache = new Cache(cacheConfig);
 * cacheManager().getObject().addCache(testCache);
 * 
 * return new EhCacheCacheManager(cacheManager().getObject()); } }
 */
